syntax = "proto3";

option go_package = "crawler/pb";

service Crawler {
  // Stream all the peers known to support the telemetry protocol including
  // the ones already known before this rpc
  rpc Subscribe(SubscribeRequest) returns (stream SubscribeItem) {}
}

message SubscribeRequest {}
message SubscribeItem { string peer_id = 1; }
